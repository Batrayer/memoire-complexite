int nombre_aleatoire(int n)
{
    n = n + 1;
    n++;
    int c;
    if (n > 1) {
        // return rand() % n + 1;    
        n = n % 5;
    }
    for (n = 0; n * n < 10; n = n + 1) {
        c = n * 2;
    }
    for (y = 0; y > 5 + n; y++) {
        c = c + 1;
    }
}